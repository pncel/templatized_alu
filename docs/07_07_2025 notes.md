named tuple over dataclass so that it is immutable
class called named tuple so replace dataclass with namedtuple
tuple instead of list opcode

Output should be based on user datatype desire
thus if user wants a boolean output, alu should output a boolean

integer values for enums as opposed to strings to save memory 