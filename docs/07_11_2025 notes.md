import isa folder(in dora)
see how archtype is defined as a class that has an optype, name, input and outputs
name can be anything
inputs and outputs are mapped from some string to an optype

inputs are inputs to the alu and want to match them to some datatype. In the ALU what data type operations does a certain op handle
multiple operation types if for example, add can handle int 16 and int 32, should have multiple ops like add16, add32