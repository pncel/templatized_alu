As opposed a constraint JSON file, it will take in objects to parse through for generation. 
Enable programming to describe hardware as opposed to generating a constraint JSON file.
Object (some memory address where data is stored)

PRGA.py to find how to pass objects into generate